{% extends "base.html.twig" %}

{% block title %}Mise à jour de mot de passe{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-3"></div>
            <div class="col-6 bg-light">
                <h1>Modification du mot de passe</h1>

                {{ form_start(form) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-primary">Enregistrer le nouveau mot de passe</button>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}