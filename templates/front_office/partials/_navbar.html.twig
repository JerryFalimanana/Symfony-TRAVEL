<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
<div class="container-fluid">
  <a class="navbar-brand" href="{{ path("home") }}">SymfonyApp</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav me-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path("home") }}">Accueil</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path("ads_index") }}">Annonces</span></a>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      {% if app.user %}
        <li class="nav-item">
          <a href="{{ path("ads_new") }}" class="nav-link">Créer une annonce</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" id="accountDropdownLink" href="#" data-toggle="dropdown">
            <img src="{{ app.user.picture }}" alt="Avatar de {{ app.user.firstName }} {{ app.user.lastName }}" class="avatar avatar-mini"> {{ app.user.firstName }} {{ app.user.lastName }}
          </a>
          
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
            <a href="{{ path("account_myAccount") }}" class="dropdown-item">Mon compte</a>
            <a href="{{ path("account_edit") }}" class="dropdown-item">Modifier mon profil</a>
            <div class="dropdown-divider"></div>
            <a href="{{ path("account_logout") }}" class="dropdown-item">Déconnexion</a>
          </div>
        </li>
      {% else %}
        <li class="nav-item">
          <a href="{{ path("account_register") }}" class="nav-link">Inscription</a>
        </li>
        <li class="nav-item">
          <a href="{{ path("account_login") }}" class="nav-link">Connexion</a>
        </li>
      {% endif %}
    </ul>
  </div>
</div>
</nav>