<nav class="navbar navbar-expand-lg navbar-dark bg-info">
<div class="container-fluid">
  <a class="navbar-brand" href="{{ path("home") }}">SymfonyApp</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav me-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path("home") }}">Accueil</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path("ads_index") }}">Annonces</span></a>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      {% if app.user %}
        <li class="nav-item">
          <a href="{{ path("ads_new") }}" class="nav-link">
            Créer une annonce
          </a>
        </li>
        <li class="nav-item">
          <a href="{{ path("account_bookings") }}" class="nav-link">
            Mes réservations
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path("account_myAccount") }}">
            <img src="{{ app.user.picture }}" alt="Avatar de {{ app.user.firstName }} {{ app.user.lastName }}" class="avatar avatar-mini"> {{ app.user.firstName }} {{ app.user.lastName }}
          </a>
        </li>
        <li class="nav-item ml-3">
          <a href="{{ path("account_logout") }}" class="nav-link">
            Déconnexion  <i class="fas fa-power-off"></i>
          </a>
        </li>
      {% else %}
        <li class="nav-item">
          <a href="{{ path("account_register") }}" class="nav-link">Inscription</a>
        </li>
        <li class="nav-item">
          <a href="{{ path("account_login") }}" class="nav-link">Connexion</a>
        </li>
      {% endif %}
    </ul>
  </div>
</div>
</nav>